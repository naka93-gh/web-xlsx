name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # ライブラリ実行下限は 20.12（engines）だが、テスト実行系（vitest/vite/rolldown）は
        # Node ^20.19 || >=22.12 を要求するため、CI の 20.x 下限は 20.19 に合わせる
        node-version: ["20.19", "22", "24"]
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      # dist が「バンドラ無しの Node ESM」で動くか（拡張子なし import 等の混入検知）
      - run: pnpm test:dist

  # バンドルサイズ LIMIT（read+write 合算 gzip 10KB） としてチェック
  # Node 版に依らないので単発実行
  size:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm size:check

  # ブランチ保護の必須チェックを安定させるゲート。
  # check は matrix 展開で名前が変わる（"check (22)" 等）ため必須コンテキストに
  # 直接指定すると食い違う。必須チェックはこの gate 1 本だけを指定すれば、
  # matrix や Node 範囲（0.4.0 で 22+ に絞る予定）を変えても保護設定は無修正で済む。
  gate:
    if: always()
    needs: [check, size]
    runs-on: ubuntu-latest
    steps:
      - name: Required jobs succeeded
        if: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}
        run: exit 1
